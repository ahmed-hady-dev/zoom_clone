import 'package:flutter/material.dart';

class HistoryMeetingView extends StatelessWidget {
  const HistoryMeetingView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('HistoryMeetingView'),
    );
  }
}
